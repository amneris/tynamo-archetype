<!-- Need this to be able to redeploy on Windoze -->
<Context path="/${D}{project.build.finalName}"
		 docBase="${D}{project.build.directory}/${D}{project.build.finalName}"
		 debug="99"
		 reloadable="true"
		 autoDeploy="true"
		 antiJARLocking="true"
		 antiResourceLocking="true">

</Context>
